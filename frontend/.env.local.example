# Leave empty so the frontend calls same-origin /api/* paths.
# Next.js rewrites (see next.config.js) proxy those to the backend,
# so the whole app works from ONE link (e.g. http://192.168.1.4:3000)
# regardless of whether you open it via localhost or a LAN IP.
NEXT_PUBLIC_API_URL=
